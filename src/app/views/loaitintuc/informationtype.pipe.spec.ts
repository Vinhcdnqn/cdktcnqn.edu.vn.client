import { InformationtypePipe } from './informationtype.pipe';

describe('InformationtypePipe', () => {
  it('create an instance', () => {
    const pipe = new InformationtypePipe();
    expect(pipe).toBeTruthy();
  });
});
